/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getArrayDecoder, getArrayEncoder, getBooleanDecoder, getBooleanEncoder, getBytesDecoder, getBytesEncoder, getI16Decoder, getI16Encoder, getI64Decoder, getI64Encoder, getStructDecoder, getStructEncoder, getU16Decoder, getU16Encoder, getU32Decoder, getU32Encoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder, type Account, type Address, type EncodedAccount, type FetchAccountConfig, type FetchAccountsConfig, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';

export const POOL_STATE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([247, 237, 227, 245, 215, 195, 222, 70]);

export function getPoolStateDiscriminatorBytes(): ReadonlyUint8Array { return fixEncoderSize(getBytesEncoder(), 8).encode(POOL_STATE_DISCRIMINATOR); }

export type PoolState = { discriminator: ReadonlyUint8Array; 
/** Which config the pool belongs */
ammConfig: Address; 
/** pool creator */
poolCreator: Address; 
/** Token A */
token0Vault: Address; 
/** Token B */
token1Vault: Address; 
/**
 * Pool tokens are issued when A or B tokens are deposited.
 * Pool tokens can be withdrawn back to the original A or B token.
 */
lpMint: Address; 
/** Mint information for token A */
token0Mint: Address; 
/** Mint information for token B */
token1Mint: Address; 
/** token_0 program */
token0Program: Address; 
/** token_1 program */
token1Program: Address; 
/** observation account to store oracle data */
observationKey: Address; authBump: number; 
/**
 * Bitwise representation of the state of the pool
 * bit0, 1: disable deposit(value is 1), 0: normal
 * bit1, 1: disable withdraw(value is 2), 0: normal
 * bit2, 1: disable swap(value is 4), 0: normal
 */
status: number; lpMintDecimals: number; 
/** mint0 and mint1 decimals */
mint0Decimals: number; mint1Decimals: number; 
/** True circulating supply without burns and lock ups */
lpSupply: bigint; 
/** The amounts of token_0 and token_1 that are owed to the liquidity provider. */
protocolFeesToken0: bigint; protocolFeesToken1: bigint; fundFeesToken0: bigint; fundFeesToken1: bigint; 
/** The timestamp allowed for swap in the pool. */
openTime: bigint; 
/** recent epoch */
recentEpoch: bigint; 
/**
 * Creator fee collect mode
 * 0: both token_0 and token_1 can be used as trade fees. It depends on what the input token is when swapping
 * 1: only token_0 as trade fee
 * 2: only token_1 as trade fee
 */
creatorFeeOn: number; enableCreatorFee: boolean; padding1: ReadonlyUint8Array; creatorFeesToken0: bigint; creatorFeesToken1: bigint; 
/**
 * Pyth Lazer price feed id tracked by this pool (e.g. SOL/USD = 1).
 * Enforced non-zero at pool creation; the swap instruction strictly requires the
 * incoming signed payload's feed id to match this value.
 */
pythPriceFeedId: number; 
/**
 * Minimum total spread in basis points (40 = 0.40% total, 20 bps each side
 * of the oracle mid). Acts as a floor — Pyth's best_bid / best_ask override
 * this value whenever the market signals a wider spread.
 */
minSpreadBps: number; 
/**
 * Inventory-skew on/off switch (0 = disabled, 1 = enabled). Stored as u8
 * for stable zero-copy layout; bools aren't guaranteed 1 byte.
 */
inventorySkewEnabled: number; reserved2: number; 
/**
 * Inside this imbalance (bps of value), no skew is applied. Prevents
 * micro-oscillation when the pool is near 50/50.
 */
inventorySkewDeadzoneBps: number; 
/** Skew in bps applied per 1% (100 bps) of imbalance beyond the dead zone. */
inventorySkewBpsPerPct: number; 
/** Hard cap on |skew_bps| regardless of imbalance magnitude. */
inventorySkewMaxBps: number; reserved3: number; 
/**
 * Per-pool keeper bot pubkey. The only signer (besides the global admin
 * rotating it) allowed to call `update_pool_oracle`.
 */
oracleKeeper: Address; 
/**
 * Bot-pushed effective bid mantissa (canonical token_1/token_0 direction,
 * same convention as the source Pyth Lazer feed). 0 = oracle not seeded.
 */
effectiveBidMantissa: bigint; 
/**
 * Bot-pushed effective ask mantissa (canonical direction, paired with
 * `effective_bid_mantissa`). Always >= `effective_bid_mantissa` once seeded.
 */
effectiveAskMantissa: bigint; 
/** Pyth exponent for the bid/ask mantissas above. */
priceExponent: number; oraclePad: ReadonlyUint8Array; 
/**
 * Bot-pushed dynamic fee rate (FEE_RATE_DENOMINATOR_VALUE-scaled). When 0,
 * the swap path falls back to `amm_config.trade_fee_rate`, so a freshly-
 * initialized pool keeps trading at the static rate until the bot's
 * first push.
 */
dynamicFeeRate: bigint; 
/**
 * Slot at which the oracle cache was last refreshed. Telemetry only —
 * the chain does not enforce freshness.
 */
lastOracleUpdateSlot: bigint; 
/** Unix timestamp of the last oracle refresh. Telemetry only. */
lastOracleUpdateUnix: bigint; 
/** padding for future updates */
padding: Array<bigint>;  };

export type PoolStateArgs = { 
/** Which config the pool belongs */
ammConfig: Address; 
/** pool creator */
poolCreator: Address; 
/** Token A */
token0Vault: Address; 
/** Token B */
token1Vault: Address; 
/**
 * Pool tokens are issued when A or B tokens are deposited.
 * Pool tokens can be withdrawn back to the original A or B token.
 */
lpMint: Address; 
/** Mint information for token A */
token0Mint: Address; 
/** Mint information for token B */
token1Mint: Address; 
/** token_0 program */
token0Program: Address; 
/** token_1 program */
token1Program: Address; 
/** observation account to store oracle data */
observationKey: Address; authBump: number; 
/**
 * Bitwise representation of the state of the pool
 * bit0, 1: disable deposit(value is 1), 0: normal
 * bit1, 1: disable withdraw(value is 2), 0: normal
 * bit2, 1: disable swap(value is 4), 0: normal
 */
status: number; lpMintDecimals: number; 
/** mint0 and mint1 decimals */
mint0Decimals: number; mint1Decimals: number; 
/** True circulating supply without burns and lock ups */
lpSupply: number | bigint; 
/** The amounts of token_0 and token_1 that are owed to the liquidity provider. */
protocolFeesToken0: number | bigint; protocolFeesToken1: number | bigint; fundFeesToken0: number | bigint; fundFeesToken1: number | bigint; 
/** The timestamp allowed for swap in the pool. */
openTime: number | bigint; 
/** recent epoch */
recentEpoch: number | bigint; 
/**
 * Creator fee collect mode
 * 0: both token_0 and token_1 can be used as trade fees. It depends on what the input token is when swapping
 * 1: only token_0 as trade fee
 * 2: only token_1 as trade fee
 */
creatorFeeOn: number; enableCreatorFee: boolean; padding1: ReadonlyUint8Array; creatorFeesToken0: number | bigint; creatorFeesToken1: number | bigint; 
/**
 * Pyth Lazer price feed id tracked by this pool (e.g. SOL/USD = 1).
 * Enforced non-zero at pool creation; the swap instruction strictly requires the
 * incoming signed payload's feed id to match this value.
 */
pythPriceFeedId: number; 
/**
 * Minimum total spread in basis points (40 = 0.40% total, 20 bps each side
 * of the oracle mid). Acts as a floor — Pyth's best_bid / best_ask override
 * this value whenever the market signals a wider spread.
 */
minSpreadBps: number; 
/**
 * Inventory-skew on/off switch (0 = disabled, 1 = enabled). Stored as u8
 * for stable zero-copy layout; bools aren't guaranteed 1 byte.
 */
inventorySkewEnabled: number; reserved2: number; 
/**
 * Inside this imbalance (bps of value), no skew is applied. Prevents
 * micro-oscillation when the pool is near 50/50.
 */
inventorySkewDeadzoneBps: number; 
/** Skew in bps applied per 1% (100 bps) of imbalance beyond the dead zone. */
inventorySkewBpsPerPct: number; 
/** Hard cap on |skew_bps| regardless of imbalance magnitude. */
inventorySkewMaxBps: number; reserved3: number; 
/**
 * Per-pool keeper bot pubkey. The only signer (besides the global admin
 * rotating it) allowed to call `update_pool_oracle`.
 */
oracleKeeper: Address; 
/**
 * Bot-pushed effective bid mantissa (canonical token_1/token_0 direction,
 * same convention as the source Pyth Lazer feed). 0 = oracle not seeded.
 */
effectiveBidMantissa: number | bigint; 
/**
 * Bot-pushed effective ask mantissa (canonical direction, paired with
 * `effective_bid_mantissa`). Always >= `effective_bid_mantissa` once seeded.
 */
effectiveAskMantissa: number | bigint; 
/** Pyth exponent for the bid/ask mantissas above. */
priceExponent: number; oraclePad: ReadonlyUint8Array; 
/**
 * Bot-pushed dynamic fee rate (FEE_RATE_DENOMINATOR_VALUE-scaled). When 0,
 * the swap path falls back to `amm_config.trade_fee_rate`, so a freshly-
 * initialized pool keeps trading at the static rate until the bot's
 * first push.
 */
dynamicFeeRate: number | bigint; 
/**
 * Slot at which the oracle cache was last refreshed. Telemetry only —
 * the chain does not enforce freshness.
 */
lastOracleUpdateSlot: number | bigint; 
/** Unix timestamp of the last oracle refresh. Telemetry only. */
lastOracleUpdateUnix: number | bigint; 
/** padding for future updates */
padding: Array<number | bigint>;  };

/** Gets the encoder for {@link PoolStateArgs} account data. */
export function getPoolStateEncoder(): FixedSizeEncoder<PoolStateArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['ammConfig', getAddressEncoder()], ['poolCreator', getAddressEncoder()], ['token0Vault', getAddressEncoder()], ['token1Vault', getAddressEncoder()], ['lpMint', getAddressEncoder()], ['token0Mint', getAddressEncoder()], ['token1Mint', getAddressEncoder()], ['token0Program', getAddressEncoder()], ['token1Program', getAddressEncoder()], ['observationKey', getAddressEncoder()], ['authBump', getU8Encoder()], ['status', getU8Encoder()], ['lpMintDecimals', getU8Encoder()], ['mint0Decimals', getU8Encoder()], ['mint1Decimals', getU8Encoder()], ['lpSupply', getU64Encoder()], ['protocolFeesToken0', getU64Encoder()], ['protocolFeesToken1', getU64Encoder()], ['fundFeesToken0', getU64Encoder()], ['fundFeesToken1', getU64Encoder()], ['openTime', getU64Encoder()], ['recentEpoch', getU64Encoder()], ['creatorFeeOn', getU8Encoder()], ['enableCreatorFee', getBooleanEncoder()], ['padding1', fixEncoderSize(getBytesEncoder(), 6)], ['creatorFeesToken0', getU64Encoder()], ['creatorFeesToken1', getU64Encoder()], ['pythPriceFeedId', getU32Encoder()], ['minSpreadBps', getU16Encoder()], ['inventorySkewEnabled', getU8Encoder()], ['reserved2', getU8Encoder()], ['inventorySkewDeadzoneBps', getU16Encoder()], ['inventorySkewBpsPerPct', getU16Encoder()], ['inventorySkewMaxBps', getU16Encoder()], ['reserved3', getU16Encoder()], ['oracleKeeper', getAddressEncoder()], ['effectiveBidMantissa', getI64Encoder()], ['effectiveAskMantissa', getI64Encoder()], ['priceExponent', getI16Encoder()], ['oraclePad', fixEncoderSize(getBytesEncoder(), 6)], ['dynamicFeeRate', getU64Encoder()], ['lastOracleUpdateSlot', getU64Encoder()], ['lastOracleUpdateUnix', getI64Encoder()], ['padding', getArrayEncoder(getU64Encoder(), { size: 17 })]]), (value) => ({ ...value, discriminator: POOL_STATE_DISCRIMINATOR }));
}

/** Gets the decoder for {@link PoolState} account data. */
export function getPoolStateDecoder(): FixedSizeDecoder<PoolState> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['ammConfig', getAddressDecoder()], ['poolCreator', getAddressDecoder()], ['token0Vault', getAddressDecoder()], ['token1Vault', getAddressDecoder()], ['lpMint', getAddressDecoder()], ['token0Mint', getAddressDecoder()], ['token1Mint', getAddressDecoder()], ['token0Program', getAddressDecoder()], ['token1Program', getAddressDecoder()], ['observationKey', getAddressDecoder()], ['authBump', getU8Decoder()], ['status', getU8Decoder()], ['lpMintDecimals', getU8Decoder()], ['mint0Decimals', getU8Decoder()], ['mint1Decimals', getU8Decoder()], ['lpSupply', getU64Decoder()], ['protocolFeesToken0', getU64Decoder()], ['protocolFeesToken1', getU64Decoder()], ['fundFeesToken0', getU64Decoder()], ['fundFeesToken1', getU64Decoder()], ['openTime', getU64Decoder()], ['recentEpoch', getU64Decoder()], ['creatorFeeOn', getU8Decoder()], ['enableCreatorFee', getBooleanDecoder()], ['padding1', fixDecoderSize(getBytesDecoder(), 6)], ['creatorFeesToken0', getU64Decoder()], ['creatorFeesToken1', getU64Decoder()], ['pythPriceFeedId', getU32Decoder()], ['minSpreadBps', getU16Decoder()], ['inventorySkewEnabled', getU8Decoder()], ['reserved2', getU8Decoder()], ['inventorySkewDeadzoneBps', getU16Decoder()], ['inventorySkewBpsPerPct', getU16Decoder()], ['inventorySkewMaxBps', getU16Decoder()], ['reserved3', getU16Decoder()], ['oracleKeeper', getAddressDecoder()], ['effectiveBidMantissa', getI64Decoder()], ['effectiveAskMantissa', getI64Decoder()], ['priceExponent', getI16Decoder()], ['oraclePad', fixDecoderSize(getBytesDecoder(), 6)], ['dynamicFeeRate', getU64Decoder()], ['lastOracleUpdateSlot', getU64Decoder()], ['lastOracleUpdateUnix', getI64Decoder()], ['padding', getArrayDecoder(getU64Decoder(), { size: 17 })]]);
}

/** Gets the codec for {@link PoolState} account data. */
export function getPoolStateCodec(): FixedSizeCodec<PoolStateArgs, PoolState> {
    return combineCodec(getPoolStateEncoder(), getPoolStateDecoder());
}

export function decodePoolState<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<PoolState, TAddress>;
export function decodePoolState<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<PoolState, TAddress>;
export function decodePoolState<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>): Account<PoolState, TAddress> | MaybeAccount<PoolState, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getPoolStateDecoder());
}

export async function fetchPoolState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<PoolState, TAddress>> {
  const maybeAccount = await fetchMaybePoolState(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybePoolState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<PoolState, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodePoolState(maybeAccount);
}

export async function fetchAllPoolState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<PoolState>[]> {
  const maybeAccounts = await fetchAllMaybePoolState(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybePoolState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<PoolState>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodePoolState(maybeAccount));
}

export function getPoolStateSize(): number {
  return 645;
}